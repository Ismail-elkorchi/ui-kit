{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://uik.local/schemas/contracts-components.schema.json",
  "title": "UIK Primitive Contracts Registry",
  "type": "object",
  "required": ["$schema", "schemaVersion", "components"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "schemaVersion": {
      "type": "string"
    },
    "components": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/componentEntry"
      }
    }
  },
  "$defs": {
    "stringList": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "componentEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "tagName",
        "summary",
        "attributes",
        "slots",
        "parts",
        "events",
        "a11y",
        "cssVars"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["component"]
        },
        "id": {
          "type": "string"
        },
        "tagName": {
          "type": "string"
        },
        "summary": {
          "type": "string",
          "minLength": 1
        },
        "attributes": {
          "$ref": "#/$defs/stringList"
        },
        "slots": {
          "$ref": "#/$defs/stringList"
        },
        "parts": {
          "$ref": "#/$defs/stringList"
        },
        "events": {
          "$ref": "#/$defs/stringList"
        },
        "a11y": {
          "$ref": "#/$defs/stringList"
        },
        "cssVars": {
          "$ref": "#/$defs/stringList"
        },
        "notes": {
          "$ref": "#/$defs/stringList"
        }
      }
    }
  }
}
