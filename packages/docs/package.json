{
  "name": "@ismail-elkorchi/ui-docs",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "description": "UIK documentation site and dogfooding lab.",
  "author": "Ismail El Korchi",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "deps:build": "cd ../.. && npm -w packages/ui-tokens run build && npm -w packages/ui-primitives run build && npm -w packages/ui-shell run build",
    "generate:baseline": "node ../../tools/baseline/generate.mjs",
    "generate:content": "node ./tools/generate-docs-content.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest --run --project browser --config ../../vitest.config.ts --dir .",
    "prebuild": "npm run deps:build && npm run generate:baseline && npm run generate:content",
    "predev": "npm run deps:build && npm run generate:baseline && npm run generate:content",
    "pretest": "npm run deps:build && npm run generate:baseline && npm run generate:content"
  },
  "dependencies": {
    "@ismail-elkorchi/ui-primitives": "0.1.2",
    "@ismail-elkorchi/ui-shell": "0.1.2",
    "@ismail-elkorchi/ui-tokens": "0.1.2",
    "minisearch": "^7.1.0"
  },
  "devDependencies": {
    "@vitest/browser": "^4.0.16",
    "marked": "^12.0.2",
    "prismjs": "^1.29.0",
    "typescript": "^5.6.3",
    "vite": "^6.4.1",
    "vitest": "^4.0.16"
  }
}
